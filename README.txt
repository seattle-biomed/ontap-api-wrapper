This Python module wraps the NetApp API ("NetApp Manageability SDK")
to a) facilitate working with it under Python; and b) attempt to fill
in omissions or regressions in the API.

It requires the NetApp API to be functional, which must be downloaded
from <http://support.netapp.com/>.
